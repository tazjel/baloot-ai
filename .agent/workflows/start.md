---
description: Start or Restart the complete game environment (Backend + Frontend)
---

This workflow ensures a clean slate by terminating existing processes before launching the game on the correct ports (3005).


1. **Execute Restart Script**
   // turbo
   ```powershell
   scripts/restart_game.ps1
   ```

---
description: Run the optimized, high-speed automated test suite (Headless + Parallel).
---

1. Run the Turbo Automation Script
   > Runs tests in parallel, manages snapshots, and auto-opens the report.

// turbo
powershell -ExecutionPolicy Bypass -File ./scripts/test_turbo.ps1

---
description: Cleanly restart the game server and frontend
---

This workflow terminates existing python and node processes and restarts the game environment in new windows.

1. **Execute Restart Script**
   // turbo
   ```powershell
   scripts/restart_game.ps1
   ```
